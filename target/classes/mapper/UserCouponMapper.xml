<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserCouponMapper">
    <resultMap id="BaseResultMap" type="com.example.demo.pojo.entity.UserCoupon">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="coupon_id" property="couponId"/>
        <result column="status" property="status"/>
        <result column="used_order_id" property="usedOrderId"/>
        <result column="create_time" property="createTime"/>
        <result column="used_time" property="usedTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into user_coupons(user_id, coupon_id, status, create_time)
        values(#{userId}, #{couponId}, 0, now())
    </insert>

    <update id="updateStatus">
        update user_coupons 
        set status = #{status}, 
            used_order_id = #{orderId},
            used_time = CASE WHEN #{status} = 1 THEN now() ELSE used_time END
        where id = #{id}
    </update>

    <select id="listByUser" resultMap="BaseResultMap">
        select * from user_coupons 
        where user_id = #{userId}
        <if test="status != null">
            and status = #{status}
        </if>
    </select>
    
    <select id="countByUserAndCoupon" resultType="int">
        select count(1) from user_coupons where user_id = #{userId} and coupon_id = #{couponId}
    </select>

    <select id="findUnusedByUserAndCoupon" resultMap="BaseResultMap">
        select * from user_coupons
        where user_id = #{userId}
          and coupon_id = #{couponId}
          and status = 0
        order by id asc
        limit 1
    </select>

    <update id="markUsed">
        update user_coupons
        set status = 1,
            used_order_id = #{orderId},
            used_time = now()
        where id = #{id}
          and status = 0
    </update>

    <update id="revertByOrderId">
        update user_coupons
        set status = 0,
            used_order_id = null,
            used_time = null
        where used_order_id = #{orderId}
          and status = 1
    </update>
</mapper>
