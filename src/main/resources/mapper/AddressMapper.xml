<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.AddressMapper">

    <resultMap id="BaseResultMap" type="com.example.demo.pojo.entity.Address">
        <id column="address_id" property="addressId"/>
        <result column="user_id" property="userId"/>
        <result column="contact_name" property="contactName"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="address" property="address"/>
        <result column="building" property="building"/>
        <result column="room" property="room"/>
        <result column="longitude" property="longitude"/>
        <result column="latitude" property="latitude"/>
        <result column="is_default" property="isDefault"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="listByUser" resultMap="BaseResultMap">
        select * from addresses where user_id = #{userId} order by is_default desc, create_time desc
    </select>

    <select id="findById" resultMap="BaseResultMap">
        select * from addresses where address_id = #{addressId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="addressId" keyColumn="address_id">
        insert into addresses(user_id, contact_name, contact_phone, address, building, room, longitude, latitude, is_default, create_time, update_time)
        values(#{userId}, #{contactName}, #{contactPhone}, #{address}, #{building}, #{room}, #{longitude}, #{latitude}, #{isDefault}, now(), now())
    </insert>

    <update id="update">
        update addresses
        set contact_name = #{contactName},
            contact_phone = #{contactPhone},
            address = #{address},
            building = #{building},
            room = #{room},
            longitude = #{longitude},
            latitude = #{latitude},
            is_default = #{isDefault},
            update_time = now()
        where address_id = #{addressId}
    </update>

    <delete id="delete">
        delete from addresses where address_id = #{addressId}
    </delete>

    <update id="clearDefault">
        update addresses set is_default = 0 where user_id = #{userId}
    </update>

    <update id="setDefault">
        update addresses set is_default = 1 where address_id = #{addressId}
    </update>

</mapper>
