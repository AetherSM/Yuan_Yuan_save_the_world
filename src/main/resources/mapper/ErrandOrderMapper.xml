<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.ErrandOrderMapper">
    <resultMap id="BaseResultMap" type="com.example.demo.pojo.entity.ErrandOrder">
        <id column="order_id" property="orderId"/>
        <result column="order_no" property="orderNo"/>
        <result column="user_id" property="userId"/>
        <result column="runner_id" property="runnerId"/>
        <result column="errand_type" property="errandType"/>
        <result column="title" property="title"/>
        <result column="description" property="description"/>
        <result column="pickup_address" property="pickupAddress"/>
        <result column="delivery_address" property="deliveryAddress"/>
        <result column="contact_name" property="contactName"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="pickup_code" property="pickupCode"/>
        <result column="expected_time_range" property="expectedTimeRange"/>
        <result column="reward" property="reward"/>
        <result column="tip" property="tip"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="images" property="images"/>
        <result column="deadline" property="deadline"/>
        <result column="order_status" property="orderStatus"/>
        <result column="cancel_reason" property="cancelReason"/>
        <result column="remark" property="remark"/>
        <result column="accepted_at" property="acceptedAt"/>
        <result column="completed_at" property="completedAt"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="orderId">
        insert into errand_orders(
            order_no, user_id, errand_type, title, description, pickup_address, delivery_address,
            contact_name, contact_phone, pickup_code, expected_time_range, reward, tip, total_amount, 
            images, deadline, order_status, remark, create_time, update_time
        ) values (
            #{orderNo}, #{userId}, #{errandType}, #{title}, #{description}, #{pickupAddress}, #{deliveryAddress},
            #{contactName}, #{contactPhone}, #{pickupCode}, #{expectedTimeRange}, #{reward}, #{tip}, #{totalAmount},
            #{images}, #{deadline}, 1, #{remark}, now(), now()
        )
    </insert>

    <update id="updateStatus">
        update errand_orders 
        set order_status = #{status}, 
            update_time = now()
            <if test="runnerId != null">, runner_id = #{runnerId}, accepted_at = now()</if>
            <if test="status == 4">, completed_at = now()</if>
        where order_no = #{orderNo}
          <if test="runnerId != null">and order_status = 1 and runner_id is null</if>
          <if test="status == 4">and order_status in (2,3) and runner_id is not null</if>
    </update>

    <select id="findByOrderNo" resultMap="BaseResultMap">
        select * from errand_orders where order_no = #{orderNo}
    </select>

    <select id="listByStatus" resultMap="BaseResultMap">
        select * from errand_orders where order_status = #{status} order by create_time desc
    </select>
    
    <select id="listByUser" resultMap="BaseResultMap">
        select * from errand_orders
        <where>
            user_id = #{userId}
            <if test="status != null"> and order_status = #{status} </if>
        </where>
        order by create_time desc
    </select>

    <select id="listByRunner" resultMap="BaseResultMap">
        select * from errand_orders where runner_id = #{runnerId} order by create_time desc
    </select>

    <select id="search" resultMap="BaseResultMap">
        select * from errand_orders
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="runnerId != null">
                and runner_id = #{runnerId}
            </if>
            <if test="status != null">
                and order_status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

    <update id="updateStatusById">
        update errand_orders
        set order_status = #{status},
            update_time = now()
        where order_id = #{orderId}
    </update>
</mapper>
