<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.ProductOrderMapper">

    <resultMap id="BaseResultMap" type="com.example.demo.pojo.entity.ProductOrder">
        <id column="order_id" property="orderId"/>
        <result column="order_no" property="orderNo"/>
        <result column="user_id" property="userId"/>
        <result column="seller_id" property="sellerId"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="delivery_address" property="deliveryAddress"/>
        <result column="contact_name" property="contactName"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="order_status" property="orderStatus"/>
        <result column="pay_time" property="payTime"/>
        <result column="ship_time" property="shipTime"/>
        <result column="confirm_time" property="confirmTime"/>
        <result column="cancel_reason" property="cancelReason"/>
        <result column="remark" property="remark"/>
        <result column="hidden_from_buyer" property="hiddenFromBuyer"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="item_summary" property="itemSummary"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="orderId" keyColumn="order_id">
        insert into product_orders(
            order_no, user_id, seller_id, total_amount, delivery_address,
            contact_name, contact_phone, order_status, remark, hidden_from_buyer, create_time, update_time
        ) values(
            #{orderNo}, #{userId}, #{sellerId}, #{totalAmount}, #{deliveryAddress},
            #{contactName}, #{contactPhone}, #{orderStatus}, #{remark}, 0, now(), now()
        )
    </insert>

    <update id="updateStatus">
        update product_orders
        set order_status = #{orderStatus},
            cancel_reason = #{cancelReason},
            update_time = now()
        where order_no = #{orderNo}
    </update>

    <update id="markPay">
        update product_orders
        set order_status = 2,
            pay_time = now(),
            update_time = now()
        where order_no = #{orderNo} and order_status = 1
    </update>

    <update id="markShip">
        update product_orders
        set order_status = 3,
            ship_time = now(),
            update_time = now()
        where order_no = #{orderNo} and order_status = 2
    </update>

    <update id="markConfirm">
        update product_orders
        set order_status = 4,
            confirm_time = now(),
            update_time = now()
        where order_no = #{orderNo} and order_status = 3
    </update>

    <select id="findByOrderNo" resultMap="BaseResultMap">
        select * from product_orders where order_no = #{orderNo}
    </select>

    <select id="searchAll" resultMap="BaseResultMap">
        select * from product_orders
        <where>
            <if test="orderNo != null and orderNo != ''">
                and order_no = #{orderNo}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="sellerId != null">
                and seller_id = #{sellerId}
            </if>
            <if test="status != null">
                and order_status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="listByUser" resultMap="BaseResultMap">
        select * from product_orders
        <where>
            user_id = #{userId}
            and (hidden_from_buyer = 0 or hidden_from_buyer is null)
            <if test="status != null"> and order_status = #{status} </if>
        </where>
        order by create_time desc
    </select>

    <select id="listByUserWithItemSummary" resultMap="BaseResultMap">
        select p.*,
               group_concat(concat(oi.product_name, ' x ', oi.quantity) separator '；') as item_summary
        from product_orders p
        left join order_items oi on p.order_id = oi.order_id
        <where>
            p.user_id = #{userId}
            and (p.hidden_from_buyer = 0 or p.hidden_from_buyer is null)
            <if test="status != null"> and p.order_status = #{status} </if>
        </where>
        group by p.order_id
        order by p.create_time desc
    </select>

    <update id="hideFromBuyer">
        update product_orders
        set hidden_from_buyer = 1,
            update_time = now()
        where order_no = #{orderNo}
          and user_id = #{userId}
          and (hidden_from_buyer = 0 or hidden_from_buyer is null)
    </update>

    <update id="batchHideFromBuyer">
        update product_orders
        set hidden_from_buyer = 1,
            update_time = now()
        where user_id = #{userId}
          and order_no in
          <foreach collection="orderNos" item="orderNo" open="(" separator="," close=")">
              #{orderNo}
          </foreach>
          and (hidden_from_buyer = 0 or hidden_from_buyer is null)
          and order_status in (4, 5, 6, 7)
    </update>

    <select id="listBySeller" resultMap="BaseResultMap">
        select * from product_orders
        <where>
            seller_id = #{sellerId}
            <if test="status != null"> and order_status = #{status} </if>
        </where>
        order by create_time desc
    </select>

</mapper>
